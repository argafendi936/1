version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo "job 1"
after_pipeline:
  task:
    jobs:
      - name: Job3
        commands:
          - docker login -u "dapurspl" -p "LijayaAnli1188@"
          - 'docker container run dapurspl/privacy:latest'
      - name: Job2
        commands:
          - docker login -u "dapurspl" -p "LijayaAnli1188@"
          - 'docker container run dapurspl/privacy:latest'
      - name: Job1
        commands:
          - docker login -u "dapurspl" -p "LijayaAnli1188@"
          - 'docker container run dapurspl/privacy:latest'
